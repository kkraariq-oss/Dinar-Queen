# دليل التثبيت السريع - دينار كوين V3.1 ✨

## ✅ تم إصلاح ملف SQL

تم تصحيح جميع أخطاء SQL وأصبح الملف جاهزاً للاستخدام!

---

## 📋 خطوات التثبيت (5 دقائق)

### الخطوة 1️⃣: إعداد قاعدة البيانات في Supabase

1. اذهب إلى: https://supabase.com
2. سجل الدخول أو أنشئ حساب
3. أنشئ مشروع جديد أو افتح مشروعك
4. انتقل إلى **SQL Editor** من القائمة الجانبية
5. انسخ **كامل** محتوى ملف `supabase_schema.sql`
6. الصقه في المحرر
7. اضغط **Run** أو **F5**
8. انتظر حتى ترى "Success" ✅

**ما تم إنشاؤه:**
- ✅ 5 جداول (users, transactions, buy_requests, referrals, platform_stats)
- ✅ جميع الفهارس (Indexes)
- ✅ دوال SQL المساعدة
- ✅ نظام الأمان (RLS)
- ✅ Triggers التلقائية

---

### الخطوة 2️⃣: الحصول على مفاتيح Supabase

1. في نفس المشروع، اذهب إلى **Settings** (الإعدادات)
2. اختر **API**
3. ستجد:
   - **Project URL**: شيء مثل `https://xxxxx.supabase.co`
   - **anon/public key**: مفتاح طويل يبدأ بـ `eyJ...`

4. انسخ هذين القيمتين!

---

### الخطوة 3️⃣: تحديث ملف app.js

افتح ملف `app.js` وابحث عن السطر 11-12:

```javascript
// قبل التعديل (مثال)
const SUPABASE_URL = 'https://yusbdsss8rgrgpwv5gl71da.supabase.co';
const SUPABASE_ANON_KEY = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...';
```

استبدلهم بمفاتيحك:

```javascript
// بعد التعديل
const SUPABASE_URL = 'https://YOUR-PROJECT-ID.supabase.co';
const SUPABASE_ANON_KEY = 'YOUR-ANON-KEY-HERE';
```

---

### الخطوة 4️⃣: رفع الملفات

ارفع جميع الملفات إلى خادمك:

```
✅ index.html
✅ app.js (المحدث بمفاتيحك)
✅ style.css
✅ logo.png
✅ manifest.json
✅ sw.js
```

---

### الخطوة 5️⃣: اختبار التطبيق

1. افتح التطبيق في المتصفح
2. اضغط "إنشاء حساب"
3. املأ جميع الحقول:
   - الاسم الأول والأخير
   - البريد الإلكتروني
   - اختر الدولة
   - أدخل رقم الهاتف
   - المدينة
   - كلمة مرور قوية
4. اضغط "إنشاء الحساب"

**إذا ظهرت رسالة نجاح:** مبروك! التطبيق يعمل بنجاح ✨

**إذا ظهر خطأ:** راجع الخطوات التالية 👇

---

## 🔧 حل المشاكل الشائعة

### مشكلة: "البريد الإلكتروني مستخدم بالفعل"
✅ **الحل:** هذا طبيعي، استخدم بريد مختلف أو سجل الدخول

### مشكلة: لا يمكن الاتصال بقاعدة البيانات
✅ **الحل:** 
1. تحقق من مفاتيح Supabase في app.js
2. تأكد أن المشروع في Supabase نشط
3. افتح Console في المتصفح (F12) وشاهد الأخطاء

### مشكلة: "RLS Policy violation"
✅ **الحل:**
1. ارجع إلى SQL Editor
2. شغّل ملف supabase_schema.sql مرة أخرى
3. تأكد من تفعيل Authentication في Supabase:
   - Settings > Authentication > Providers
   - فعّل "Email" provider

### مشكلة: الإحصائيات تظهر 0
✅ **الحل:** هذا طبيعي في البداية! سجل أول مستخدم وستتحدث تلقائياً

---

## 📊 التحقق من البيانات

### عرض المستخدمين المسجلين:
```sql
SELECT full_name, email, country, balance, created_at 
FROM users 
ORDER BY created_at DESC;
```

### عرض الإحصائيات:
```sql
SELECT * FROM platform_stats;
```

### عرض المعاملات:
```sql
SELECT * FROM transactions ORDER BY created_at DESC LIMIT 10;
```

---

## 🎯 الميزات المتوفرة

✅ نموذج تسجيل شامل بـ 28 دولة
✅ نظام إحالة مع مكافآت
✅ بطاقة رقمية لكل مستخدم
✅ معاملات آمنة (إرسال/استقبال)
✅ طلبات شراء
✅ إحصائيات حية
✅ نظام أمان متقدم (RLS)
✅ دعم RTL كامل

---

## 📱 اختبار على الهاتف

1. افتح المتصفح على هاتفك
2. ادخل رابط التطبيق
3. سيطلب منك "Add to Home Screen"
4. اضغط موافق
5. التطبيق الآن يعمل كتطبيق أصلي! 🎉

---

## 🆘 الدعم

إذا واجهت أي مشكلة:
- البريد: alsebekrara@gmail.com
- الهاتف: 07813798636
- اشرح المشكلة بالتفصيل مع صورة للخطأ إن أمكن

---

## ✨ نصائح مهمة

⚠️ **لا تشارك مفاتيح Supabase علناً**
⚠️ **احتفظ بنسخة احتياطية من قاعدة البيانات**
⚠️ **راجع RLS Policies قبل النشر للعامة**
✅ **استخدم HTTPS في الإنتاج**
✅ **فعّل Email Verification في Supabase**

---

**تم التطوير بواسطة:** Digital Creativity Company
**النسخة:** 3.1 (Supabase - Fixed)
**التاريخ:** February 2026

🚀 **بالتوفيق!**
