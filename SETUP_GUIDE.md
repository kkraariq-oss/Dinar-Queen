# 🪙 دينار كوين V3.0 - دليل الإعداد الشامل

## 📋 نظرة عامة
تم تحديث تطبيق دينار كوين بالكامل ليعمل مع Supabase بدلاً من Firebase. يتضمن التحديث:

### ✨ الميزات الجديدة
1. **نظام تسجيل محسّن** - حقول إضافية:
   - اسم المستخدم (username)
   - رقم الهاتف مع مفتاح الدولة
   - اختيار الدولة (22 دولة عربية)
   - المدينة
   - الشارع
   - العنوان الكامل

2. **رفع صورة البروفايل**:
   - إمكانية رفع صورة شخصية
   - التخزين في Supabase Storage
   - التحديث الفوري للصورة

3. **قاعدة بيانات Supabase**:
   - جداول محسّنة مع علاقات
   - أمان على مستوى الصفوف (RLS)
   - دوال مخصصة للعمليات
   - Real-time subscriptions

## 🚀 خطوات الإعداد

### 1️⃣ إنشاء مشروع Supabase
1. اذهب إلى [Supabase](https://supabase.com)
2. سجل دخول أو أنشئ حساب جديد
3. أنشئ مشروع جديد:
   - اختر اسم المشروع: `dinar-coin`
   - اختر كلمة مرور قوية لقاعدة البيانات
   - اختر المنطقة الأقرب لك (Middle East مثلاً)
4. انتظر حتى يتم إنشاء المشروع (1-2 دقيقة)

### 2️⃣ إنشاء قاعدة البيانات
1. افتح مشروعك في Supabase
2. اذهب إلى **SQL Editor** من القائمة الجانبية
3. أنشئ Query جديدة
4. انسخ محتوى ملف `supabase_database_schema.sql` بالكامل
5. الصق في محرر SQL
6. اضغط **Run** أو **F5**
7. تأكد من ظهور رسالة نجاح

### 3️⃣ إعداد Storage لصور البروفايل
1. اذهب إلى **Storage** من القائمة الجانبية
2. اضغط **Create a new bucket**
3. املأ البيانات:
   - **Name**: `profile-pictures`
   - **Public bucket**: ✅ (مفعّل)
4. اضغط **Create bucket**
5. افتح الـ bucket واذهب إلى **Policies**
6. أضف Policy جديد:
   ```sql
   -- السماح بالقراءة للجميع
   CREATE POLICY "Public Access"
   ON storage.objects FOR SELECT
   USING (bucket_id = 'profile-pictures');
   
   -- السماح بالرفع للمستخدمين المسجلين
   CREATE POLICY "Authenticated users can upload"
   ON storage.objects FOR INSERT
   WITH CHECK (
     bucket_id = 'profile-pictures' 
     AND auth.role() = 'authenticated'
   );
   ```

### 4️⃣ الحصول على مفاتيح API
1. اذهب إلى **Settings** > **API**
2. ستجد:
   - **Project URL**: مثل `https://xxxxx.supabase.co`
   - **anon public key**: مفتاح طويل يبدأ بـ `eyJhbGc...`
3. احفظهما في مكان آمن

### 5️⃣ تحديث ملف app.js
افتح ملف `app.js` وحدّث السطور التالية:

```javascript
// ابحث عن هذه الأسطر في بداية الملف (حول السطر 15)
const SUPABASE_URL = 'ضع_هنا_Project_URL_الخاص_بك';
const SUPABASE_ANON_KEY = 'ضع_هنا_anon_public_key_الخاص_بك';
```

### 6️⃣ رفع الملفات
ارفع جميع الملفات إلى السيرفر الخاص بك:
- `index.html`
- `app.js` (المحدّث)
- `style.css`
- `manifest.json`
- `sw.js`
- `logo.png`
- `icon-192.png`
- `icon-512.png`
- `apple-touch-icon.png`
- `background.jpg`

### 7️⃣ اختبار التطبيق
1. افتح التطبيق في المتصفح
2. جرّب إنشاء حساب جديد
3. تأكد من:
   - ✅ عمل التسجيل
   - ✅ حفظ البيانات في Supabase
   - ✅ رفع صورة البروفايل
   - ✅ عمل نظام الإحالة

## 📊 جداول قاعدة البيانات

### 1. جدول users
يحتوي على جميع بيانات المستخدمين:
- المعلومات الشخصية (الاسم، البريد، اسم المستخدم)
- رقم الهاتف والدولة
- العنوان (المدينة، الشارع، العنوان الكامل)
- صورة البروفايل
- بيانات المحفظة (الرصيد، رمز الإحالة)
- بيانات البطاقة الرقمية

### 2. جدول transactions
يحتوي على جميع المعاملات:
- الإرسال والاستقبال
- طلبات الشراء
- المكافآت

### 3. جدول purchase_requests
طلبات شراء العملات من المستخدمين

### 4. جدول global_stats
الإحصائيات العامة للتطبيق

### 5. جدول sessions (اختياري)
إدارة الجلسات

## 🔒 الأمان

### Row Level Security (RLS)
تم تفعيل RLS على جميع الجداول لضمان:
- المستخدم يرى بياناته فقط
- لا يمكن تعديل بيانات المستخدمين الآخرين
- الإحصائيات العامة للقراءة فقط

### دوال آمنة
تم إنشاء دوال PostgreSQL آمنة:
- `validate_referral_code()` - التحقق من رمز الإحالة
- `process_referral()` - معالجة الإحالة ومنح المكافآت
- `update_global_stats()` - تحديث الإحصائيات العامة

## 🎨 التخصيص

### تغيير الألوان
في `style.css`:
```css
:root {
    --gold-primary: #d4af37;
    --green-primary: #1a5f4a;
    /* غيّر هذه القيم حسب رغبتك */
}
```

### تغيير الثوابت
في `app.js`:
```javascript
const PRICE_PER_COIN = 1000;      // سعر العملة بالدينار
const TOTAL_SUPPLY = 1000000;     // إجمالي العرض
const WELCOME_BONUS = 1.0;        // مكافأة الترحيب
const REFERRAL_BONUS = 0.25;      // مكافأة الإحالة
```

## 🐛 استكشاف الأخطاء

### المشكلة: لا يعمل التسجيل
**الحل**:
1. تأكد من إضافة `SUPABASE_URL` و `SUPABASE_ANON_KEY` الصحيحين
2. تأكد من تشغيل SQL Schema بالكامل
3. تحقق من Console في المتصفح للأخطاء

### المشكلة: لا يعمل رفع الصور
**الحل**:
1. تأكد من إنشاء bucket باسم `profile-pictures`
2. تأكد من تفعيل Public على الـ bucket
3. تأكد من إضافة Policies الصحيحة

### المشكلة: لا تظهر البيانات
**الحل**:
1. افتح **Table Editor** في Supabase
2. تحقق من وجود البيانات في الجداول
3. تحقق من عمل RLS Policies

## 📞 الدعم الفني
إذا واجهت أي مشاكل:
1. راجع Console في المتصفح (F12)
2. راجع Logs في Supabase
3. تأكد من إتمام جميع الخطوات

## 📝 ملاحظات مهمة

### حدود Supabase المجانية
- **Database**: 500 MB
- **Storage**: 1 GB
- **Bandwidth**: 2 GB/month
- **Real-time connections**: 200 concurrent

### نصائح للأداء
1. استخدم Indexes على الحقول المستعملة كثيراً
2. قم بعمل cleanup للصور القديمة
3. راقب استهلاك Bandwidth

### الترقية للنسخة المدفوعة
إذا كبر المشروع، يمكنك الترقية إلى:
- **Pro Plan**: $25/month
- **Team Plan**: $599/month
- أو اتصل بـ Enterprise

## 🎉 تهانينا!
الآن تطبيقك جاهز بالكامل مع:
- ✅ قاعدة بيانات قوية وآمنة
- ✅ نظام تسجيل محسّن
- ✅ رفع صور البروفايل
- ✅ Real-time updates
- ✅ أمان عالي المستوى

صنع بـ ❤️ في العراق 🇮🇶

**Digital Creativity Company**
*ثورة رقمية عراقية* 🚀
